# دليل بناء وتجميع التطبيق كبرنامج ويندوز مكتبي (.exe)
**تطوير وإعداد: صادق الظاهري 2026**  
**هاتف / واتساب: 772092700**

---

## كيف تحمي الكود المصدري وتبيع التطبيق كبرنامج (.exe)؟
عند تحزيم التطبيق بواسطة **Electron** و **electron-builder**:
1. **تشفير وحماية الكود (ASAR Archive):**
   * يتم ضغط وتشفير كل ملفات البرمجة (`HTML`, `JavaScript`, `CSS`) داخل ملف أرشيف ثنائي موحد غير قابل للقراءة اسمه `app.asar`.
   * العميل لن يرى أي ملفات برمجية ولا يستطيع فتحها أو تعديلها بأي منصة ذكاء اصطناعي أو محرر نصوص.
2. **ملف تثبيت رسمي للويندوز (Windows Installer):**
   * يحصل العميل على ملف تثبيت باسم `Paper-to-Excel-AI-Setup-2026.exe`.
   * يثبت البرنامج تلقائياً على الويندوز مثل أي برنامج عالمي، ويضع أيقونة أنيقة على سطح المكتب وقائمة ابدأ باسم: **"ورقة إلى إكسل ذكي"**.
3. **نظام التراخيص المدمج:**
   * يطلب البرنامج من العميل مفتاح الترخيص الذي تقوم أنت بتوليده له عبر زر المالك (`admin2026`).

---

## خطوات التجميع بنقرة واحدة على كمبيوترك:
1. قم بتحميل مشروعك بصيغة مضغوطة من القائمة العلوية (**Export** -> **Download as ZIP**).
2. فك الضغط عن الملف في جهاز الكمبيوتر الخاص بك.
3. اضغط مرتين على الملف:
   ```cmd
   build-windows-exe.bat
   ```
4. سيقوم الملف تلقائياً بالبناء وتجهيز ملف التثبيت خلال دقائق.
5. ادخل على المجلد الناتج باسم:
   ```
   dist-desktop/
   ```
6. ستجد ملف التثبيت الجاهز للبيع للعملاء:
   `Paper-to-Excel-AI-Setup-2026.exe`

---

## الأوامر اليدوية (إذا أردت تشغيلها عبر Terminal / CMD):
```bash
npm install --save-dev electron electron-builder
npm run build
npx electron-builder --config electron-builder.json --win
```
